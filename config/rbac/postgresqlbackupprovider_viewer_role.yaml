# This rule is not used by the project postgresql-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to postgresql.easymile.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: postgresql-operator
    app.kubernetes.io/managed-by: kustomize
  name: postgresqlbackupprovider-viewer-role
rules:
- apiGroups:
  - postgresql.easymile.com
  resources:
  - postgresqlbackupproviders
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - postgresql.easymile.com
  resources:
  - postgresqlbackupproviders/status
  verbs:
  - get
