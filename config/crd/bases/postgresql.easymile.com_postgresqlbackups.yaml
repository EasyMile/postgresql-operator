---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: postgresqlbackups.postgresql.easymile.com
spec:
  group: postgresql.easymile.com
  names:
    kind: PostgresqlBackup
    listKind: PostgresqlBackupList
    plural: postgresqlbackups
    singular: postgresqlbackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PostgresqlBackup is the Schema for the postgresqlbackups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgresqlBackupSpec defines the desired state of PostgresqlBackup.
            properties:
              backupProvider:
                description: Postgresql backup provider
                properties:
                  name:
                    description: Custom resource name
                    type: string
                  namespace:
                    description: Custom resource namespace
                    type: string
                required:
                - name
                type: object
              database:
                description: Postgresql Database
                properties:
                  name:
                    description: Custom resource name
                    type: string
                  namespace:
                    description: Custom resource namespace
                    type: string
                required:
                - name
                type: object
              schedule:
                description: Schedule for cronjob
                type: string
            required:
            - backupProvider
            - database
            - schedule
            type: object
          status:
            description: PostgresqlBackupStatus defines the observed state of PostgresqlBackup.
            properties:
              generatedName:
                description: Generated name for resources
                type: string
              message:
                description: Human-readable message indicating details about current
                  operator phase or error.
                type: string
              phase:
                description: Current phase of the operator
                type: string
              providerHash:
                description: BackupProvider spec hash
                type: string
              ready:
                description: True if all resources are in a ready state and all work
                  is done.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
